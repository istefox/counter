## Status (2026-09-06)
**Branch:** feat/codex-provider
**Last commit:** c934b5f — refactor(app): rename ClaudeLimits to AgentLimits, add Codex usage provider
**In progress:** Codex provider added on top of the Claude Code provider. App renamed ClaudeLimits → AgentLimits (bundle id it.stefer.AgentLimits). Codex data source is `codex app-server`'s JSON-RPC-over-stdio protocol (`account/rateLimits/read`, spawned fresh per refresh, no daemon dependency) — verified live end-to-end against the real account, including a PATH-resolution fix for GUI-launched apps (checks `~/.local/bin`, falls back to a login-shell `command -v`). Windows from the default `rateLimits` bucket and any `rateLimitsByLimitId` buckets are merged by duration (5h/weekly), keeping the highest percent per duration; per-bucket `limitId`/`limitName` are never shown (verified live they're opaque backend metering categories unrelated to the configured model). Gemini provider not started.
**Next:** Push branch and open a PR (same flow as PR #1: push → CI watch → merge gate). After that, verify/plan the Gemini provider (Nimbalyst's "Gemini" support is for Google Antigravity IDE, not the Gemini CLI — needs its own investigation like Codex got).
**Open decisions:** none
